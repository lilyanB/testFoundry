// SPDX-License-Identifier: MIT
pragma solidity >=0.8.0;

interface ICounterTypes {
    /**
     * @notice Example of types use in {testFunction}.
     * @param oneAddress The address.
     * @param oneUint The uint256.
     */
    struct StructTypes {
        address oneAddress;
        uint256 oneUint;
    }
}
